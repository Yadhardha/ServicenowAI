apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  namespace : aiops-test
spec:
  selector:
    app: payment-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP